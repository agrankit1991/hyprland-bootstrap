# -----------------------------------------------------
# Input Configuration
# https://wiki.hyprland.org/Configuring/Variables/#input
# -----------------------------------------------------
input {
    # --- Keyboard Layout & Options ---
    kb_layout = us
    kb_variant =
    kb_model =
    kb_options = compose:caps
    kb_rules =

    # --- Keyboard Behavior ---
    repeat_rate = 40
    repeat_delay = 600
    numlock_by_default = true

    # --- Mouse & Sensitivity ---
    follow_mouse = 1
    sensitivity = 0 # -1.0 - 1.0, 0 means no modification.

    # --- Touchpad Configuration ---
    touchpad {
        # disable natural (inverse) scrolling
        natural_scroll = false 
        
        # Control the speed of scrolling (0.4 is slower than default)
        scroll_factor = 0.4
        
        # behavior settings (from your commented out lines, left as reference)
        # clickfinger_behavior = true
    }
}

# -----------------------------------------------------
# Miscellaneous Settings
# -----------------------------------------------------
misc {
    key_press_enables_dpms = true  # key press will trigger wake
    mouse_move_enables_dpms = true # mouse move will trigger wake
}

# -----------------------------------------------------
# Window Rules
# -----------------------------------------------------

# NOTE: 'scrolltouchpad' is NOT a standard Hyprland rule. 
# It will likely cause a config error unless you have a specific plugin installed.
# I have commented them out to prevent errors. Uncomment if you are sure they are supported.

# windowrule = scrolltouchpad 1.5, class:(Alacritty|kitty)
# windowrule = scrolltouchpad 0.2, class:com.mitchellh.ghostty

# Gesture settings (Reference)
# gesture = 3, horizontal, workspace